{
  "name": "fgta",
  "private": true,
  "description": "Netlify Functions for FGTA (currently just the YouTube Live lookup that tells the site what is on air). The site itself is a static index.html with no build step, and the function has no dependencies — this file exists so Netlify recognises the project."
}
