## Adds Netlify Functions (for the AI-highlight-judge endpoint the live-stream
## point tracker calls) without touching how the static site itself is built
## or published — there's deliberately no [build] block here, so the
## publish directory stays whatever's already configured in the Netlify UI.
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
